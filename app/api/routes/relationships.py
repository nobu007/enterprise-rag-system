"""
Document relationships endpoints

Placeholder router. ``app/main.py`` imports this module and mounts
``relationships.router`` (prefix ``/api/v1``, tag ``Relationships``). This
minimal stub satisfies that import WITHOUT exposing any endpoints, so the
application boots while the Relationships feature is under development.
Replace this file with the real router when the feature is implemented.
"""

from fastapi import APIRouter

router = APIRouter()
