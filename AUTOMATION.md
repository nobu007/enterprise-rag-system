# AUTOMATION.md — Enterprise RAG System 自動化中枢

この文書は、このリポジトリで自律実行する作業の共通契約を定義する。
`LOOP.md` はこの契約に沿った実行ループの詳細、`PURPOSE.md` の
`Autonomous Intent` は TAS auto dispatch 用の機械転写である。3 文書の実行単位・
検証・禁止事項・成果物は一致させる。

## 実行単位

- 1 run = 1 改善。候補は `LOOP.md` に定める優先順で先頭の 1 件だけ選ぶ。
- 変更は必要最小限にし、既存の目的・仕様・禁止事項を弱めない。
- 30 分を超えそうな作業、依存更新、大規模リファクタ、複数領域にまたがる作業は着手せず、残課題として記録する。
- 安全な候補がない、検証できない、または時間切れの場合は変更を残さず中断レポートを出す。

## 検証

実装後、次の順でリポジトリの検証を行う。

```bash
python3 -m compileall app -q
python3 -m pytest -q
```

`tests/` が存在しない場合はテストを実行せず、その事実を報告する。既存テストを
壊した状態では commit しない。秘密・外部サービス・LLM の実呼び出しを伴う検証は
モックまたは安全なローカル検証に置き換える。

## 禁止事項

- push、force-push、履歴改変、タグ・リリース操作、deploy、公開を伴う Docker 操作
- `.env`、API キー、トークンその他の秘密の読み出し・コミット
- クラウド API、LLM API、本番サービスの実呼び出し
- 1 run で 2 件目の改善に着手すること
- 依存のメジャー更新、根拠のない capability の追加、大規模な破壊的変更
- `LOOP.md` の禁止事項や本書の実行契約を消す・弱める変更

## 成果物と Git 手順

成功した run は、最小変更・検証結果・残課題を成果物とする。変更を確認した後、
変更ファイルだけを明示して 1 件のローカル commit を作成する。

```bash
git diff
git add <変更ファイル>
git commit -m "<変更内容の簡潔な説明>"
git status --short
```

push は行わない。chain 完了後の反映は harness に委譲する。管理用ログだけの差分や
実質的な改善を含まない差分は commit しない。

## 完了報告

最終報告には、変更ファイル、実行した検証コマンドと結果、残課題、次の候補を含める。
変更がない場合は、中断理由と確認した事実を明記する。
